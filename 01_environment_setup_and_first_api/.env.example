# Copy to `.env` in this folder and edit. `.env` is gitignored — real secrets
# never belong in the repository, and the example file documents what exists.
SHELFSPACE_APP_NAME="Shelfspace API"
SHELFSPACE_ENVIRONMENT=development
SHELFSPACE_DEBUG=true
SHELFSPACE_HOST=127.0.0.1
SHELFSPACE_PORT=8001
